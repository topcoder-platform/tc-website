machine:
  services:
    - docker

checkout:
  pre:
    - git clone https://github.com/topcoder-platform/tc-website-glue glue
    - git clone https://github.com/topcoder-platform/tc-website-shared shared
    # for review 
    #- git clone https://github.com/<username>/temp-maven-repo
    #- git clone -b svn2git https://github.com/<username>/tc-website-glue glue


test:
  override:
    - cp -Rf .deploy/circleci/* . && docker run -v ${HOME}:/root/tc-platform appiriodevops/tc-website:build
    #for review
    #- cp -Rf .deploy/circleci/* . && docker run -v ${HOME}:/root/tc-platform <your_tc-website_tag>:build
~                                                                                                                                                                        
~                                                                                                                                                                        
~                                                                                                  
