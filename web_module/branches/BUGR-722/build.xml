<?xml version="1.0" encoding="ISO-8859-1"?>
<project name="web" basedir="." default="artifacts">
    <property name="root.dir" location="${basedir}/.." />
    <property name="glue.dir" location="${root.dir}/glue" />
    <import file="${glue.dir}/glue-jar.xml" />

    
</project>
